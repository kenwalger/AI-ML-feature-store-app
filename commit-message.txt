Fix Heroku build and add deployment documentation

This commit fixes the Heroku build failure and adds comprehensive
deployment documentation with required addon specifications.

Build Fixes:
- Remove bin/pre_compile script that was causing PATH issues
- Rely on uv-heroku-buildpack for automatic uv installation
- Remove problematic release command from Procfile
- Buildpack now handles uv installation automatically

Deployment Documentation:
- Add DEPLOYMENT.md with detailed addon setup instructions
- Document required vs optional addons clearly
- Include troubleshooting section for common issues
- Add cost considerations for demo deployments

Configuration Updates:
- Update app.json to include heroku-ai addon in addons list
- Update README with clearer addon provisioning instructions
- Clarify that buildpack handles uv installation automatically

Required Addons:
- Heroku Postgres (essential-0) - Primary database
- Heroku Redis (mini) - For Celery background jobs
- Heroku Managed Inference (basic) - For Cohere embeddings

Optional Addons:
- Heroku Postgres Follower - For demonstrating follower pools

The build should now succeed as the uv-heroku-buildpack automatically
installs and configures uv without requiring manual PATH manipulation.

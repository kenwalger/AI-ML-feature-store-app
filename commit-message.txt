Fix stats endpoint NameError for follower_engine

The stats endpoint was failing with NameError because metrics.py was
still referencing the old follower_engine module variable, which was
refactored to use lazy initialization functions.

Fix:
- Update metrics service to use _create_follower_engine() function
- Add proper error handling for follower engine availability check
- Stats endpoint now works correctly without runtime errors

This resolves the 500 errors on /api/stats that were causing console
errors in the frontend.
